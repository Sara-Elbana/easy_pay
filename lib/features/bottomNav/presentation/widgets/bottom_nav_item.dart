class BottomNavItem {
  final String icon;
  final String activeIcon;
  final String title;

  const BottomNavItem({
    required this.icon,
    required this.activeIcon,
    required this.title,
  });
}